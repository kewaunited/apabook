- if object.errors.any?
  %div{ id: 'error_explanation' }
  %div{ class: flash_class('alert') } The form contains #{pluralize(object.errors.count, 'error')}.
  %ul
    - object.errors.full_messages.each do |msg|
      %li= msg
